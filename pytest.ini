[pytest]
# 只自动收集 tests/ 下的用例。
# tools/verification/ 里的 test_*.py 是**可重复执行的脚本**（没有 pytest 用例函数，
# 需要真实 exe / 会占用托盘 / 耗时较长），不属于自动化测试，所以排除在收集之外。
# 要单独跑它们请直接：`python tools/verification/test_launcher.py --clean`
testpaths = tests
